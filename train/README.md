# Train

Purpose: train and evaluate models in Azure ML and run backtests.

Notes:
- Keep training reproducible with tracked datasets and parameters.
- Save artifacts for downstream reporting in the UI.
